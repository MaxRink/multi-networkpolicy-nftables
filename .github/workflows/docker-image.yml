name: docker-image
on:
  push:
    tags:
      - v*
    branches:
      - master
      - nftables
      - test-ci
      - feat/controller-runtime-rearchitect
jobs:
  build-images:
    name: Docker build
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        arch: ["amd64", "arm64"]
        include:
          - runner: ubuntu-latest
            arch: "amd64"
          - runner: ubuntu-24.04-arm
            arch: "arm64"
    env:
      GO111MODULE: on
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v4

      - name: Normalize image repository
        run: echo "IMAGE_REPOSITORY=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_REPOSITORY }}
          flavor:
            latest=false
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=pr-76,enable=${{ github.ref == 'refs/heads/feat/controller-runtime-rearchitect' }}
            type=sha,prefix=pr-76-,format=short,enable=${{ github.ref == 'refs/heads/feat/controller-runtime-rearchitect' }}

      - name: Build and Push Docker Image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
          tags: ${{ env.IMAGE_REPOSITORY }}
          labels: ${{ steps.meta.outputs.labels }}
          file: Dockerfile

      - name: Export digest
        run: |
          mkdir -p ${{ runner.temp }}/digests-${{ github.repository_id }}
          digest="${{ steps.build.outputs.digest }}"
          touch "${{ runner.temp }}/digests-${{ github.repository_id }}/${digest#sha256:}"

      - name: Upload digest
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ github.repository_id }}-${{ matrix.arch }}
          path: ${{ runner.temp }}/digests-${{ github.repository_id }}/*
          if-no-files-found: error
          retention-days: 1

  publish-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    needs:
      - build-images
    steps:
      - name: Normalize image repository
        run: echo "IMAGE_REPOSITORY=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-${{ github.repository_id }}-*
          merge-multiple: true

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3.0.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5.5.1
        with:
          images: ${{ env.IMAGE_REPOSITORY }}
          flavor:
            latest=false
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=pr-76,enable=${{ github.ref == 'refs/heads/feat/controller-runtime-rearchitect' }}
            type=sha,prefix=pr-76-,format=short,enable=${{ github.ref == 'refs/heads/feat/controller-runtime-rearchitect' }}

      - name: Create manifest list and push
        working-directory: ${{ runner.temp }}/digests
        run: |
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf "${IMAGE_REPOSITORY}@sha256:%s " *)
