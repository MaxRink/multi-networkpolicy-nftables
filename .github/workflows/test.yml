on:
  push:
    branches:
      - main
      - master
      - nftables
  pull_request:
name: test
permissions:
  contents: read
jobs:
  test:
    strategy:
      matrix:
        go-version: ["1.25.13"]
        os: [ubuntu-24.04]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ matrix.go-version }}
        cache: true

    - name: Run Revive Action by pulling pre-built image
      uses: docker://morphy/revive-action:v2
      with:
        exclude: "./vendor/..."

    - name: Run go fmt
      run: make fmt

    - name: Run go vet
      run: make vet

    - name: Run govulncheck
      run: make govulncheck

    - name: Run go env toolchain
      run: go env -w GOTOOLCHAIN=go${{ matrix.go-version }}+auto

    - name: Install envtest assets
      run: |
        go install sigs.k8s.io/controller-runtime/tools/setup-envtest@v0.24.1
        mkdir -p testbin/k8s
        echo "KUBEBUILDER_ASSETS=$(setup-envtest use 1.35.0 --bin-dir testbin/k8s -p path)" >> "$GITHUB_ENV"

    - name: Load kernel modules
      run: sudo modprobe nft_ct

    - name: Test
      run: make test

    - name: Send coverage
      uses: shogo82148/actions-goveralls@v1
      continue-on-error: true
      with:
        path-to-profile: profile.cov
        flag-name: "Go-${{ matrix.go-version }}-${{ matrix.os }}"
        parallel: true

  # notifies that all test jobs are finished.
  finish:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: shogo82148/actions-goveralls@v1
        continue-on-error: true
        with:
          parallel-finished: true
